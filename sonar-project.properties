sonar.projectKey=central-platform-backend
sonar.projectName=Central Platform Backend
sonar.projectVersion=1.0

sonar.sources=backend-services/users-api
sonar.exclusions=**/__pycache__/**,**/*.pyc,**/migrations/**

sonar.python.version=3.11

# Quality gate thresholds enforced in SonarQube server settings.
# Pipeline will fail if gate status is not "OK".
sonar.qualitygate.wait=true
