apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-api-prod
  namespace: argocd
  labels:
    environment: prod
    project: central-platform
spec:
  project: prod
  source:
    repoURL: https://github.com/Avenis3010/ignite-solutions.git
    targetRevision: production
    path: backend-services/users-api/backend-api
    helm:
      valueFiles:
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false   # namespace is pre-created by k8s/prod/namespace.yaml
